const accountId = 1234
let accountEmail = "manudb@gppgle.com"
var accountPassword = "12345"
accountCity = "Jammu"

/*-> const is used to declare a variable which contain constant value only
 ->let and var is used to declare the variables 
 ->we mostly use 'let' declare the variable because of issue in block scope and function scope
 -> Also if we use 'var' to declare the variable then it will make it global
 -> we also declare a variable without using any keyword*/


console.table([accountCity,accountEmail,accountId,accountPassword])
// this function is used so that we haven't need to write console separately for all printing all values